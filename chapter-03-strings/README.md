# Chapter 3 — Strings

Working with Python strings: indexing, slicing, built-in string methods, and escape sequences.

## Concepts
- `01_intro_to_string.py` — string basics and indexing
- `02_negative_slicing.py` — slicing strings, including negative indices
- `03_str_functions.py` — common string methods (`upper`, `lower`, `find`, `replace`, `split`, `join`, etc.)
- `04_escape_sep.py` — escape characters and separators

## Practice Problems
5 problems applying string manipulation and built-in methods.
