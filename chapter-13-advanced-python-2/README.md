# Chapter 13 — Advanced Python 2

Functional programming tools and advanced string/data handling in Python.

## Concepts
- `01_venv.py` — virtual environments and package isolation
- `02_lambda_function.py` — anonymous functions with `lambda`
- `03_join_method.py` — the `str.join()` method for efficient string building
- `04_format_method.py` — string formatting with `str.format()` and f-strings
- `05_map_filter_reduce.py` — functional tools: `map()`, `filter()`, and `reduce()`

## Practice Problems
7 problems applying lambda functions, `map`/`filter`/`reduce`, and string formatting.
